import numpy as pn
N,M = map(int,input().split())
L = lambda a,b: ([a+b,a-b,a*b,a//b,a%b,a**b])
A,B =[pn.array([list(map(int,input().split()))for _ in range(N)]) for i in range(2)]
[print(f"{c}") for c in L(A,B)]
